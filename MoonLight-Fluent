local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local Window = Fluent:CreateWindow({
    Title = "MoonHub " .. Fluent.Version,
    SubTitle = "by Moonlight×Godlike",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 460),
    Acrylic = true, -- The blur may be detectable, setting this to false disables blur entirely
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl -- Used when theres no MinimizeKeybind
})

-- Fluent provides Lucide Icons, they are optional
local Tabs = {
    Main = Window:AddTab({ Title = "Main", Icon = "anchor" }),
    Main = Window:AddTab({ Title = "Auto Weight", Icon = "clock" }),
    Main = Window:AddTab({ Title = "TP Gyms", Icon = "cog" }),
    Main = Window:AddTab({ Title = "Auto Rebirth, Icon = "codesandbox" }),
    Main = Window:AddTab({ Title = "Auto Kill", Icon = "untensils-crossed" }),
    Main = Window:AddTab({ Title = "OP tools", Icon = "circle-alert" }),
    Main = Window:AddTab({ Title = "Credits", Icon = "badge-check" }),
}

