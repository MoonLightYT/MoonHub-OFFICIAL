local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local Window = Fluent:CreateWindow({
    Title = "MoonHub " .. Fluent.Version,
    SubTitle = "by Moonlight×Godlike",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 460),
    Acrylic = true,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl
})

-- Tabs Setup
local Tabs = {}
Tabs.Main = Window:AddTab({
    Title = "Main",
    Icon = "Anchor"
})

-- Add sections to Main tab
local Section1 = Tabs.Main:AddSection("Welcome to MoonHub")

-- Add paragraph to a section
Section1:AddParagraph({
    Title = "This is where you can find various different functions!!!"
})

-- Other tabs
Tabs.AutoWeight = Window:AddTab({
    Title = "Auto Weight",
    Icon = "Muscle"
})

Tabs.TPGyms = Window:AddTab({
    Title = "TP Gyms",
    Icon = "Clock"
})

Tabs.AutoRebirth
