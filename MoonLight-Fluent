local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local Window = Fluent:CreateWindow({
    Title = "MoonHub " .. Fluent.Version,
    SubTitle = "by Moonlight×Godlike",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 460),
    Acrylic = true,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl
})

-- Tabs Setup
local Tabs = {}
Tabs.Main = Window:AddTab({
    Title = "Main",
    Icon = "Anchor",
})            
})

-- Other tabs
Tabs.AutoWeight = Window:AddTab({
    Title = "Auto Weight",
    Icon = "Muscle"
})

Tabs.TPGyms = Window:AddTab({
    Title = "TP Gyms",
    Icon = "Clock"
})

Tabs.AutoRebirth = Window:AddTab({
    Title = "Auto Rebirth",
    Icon = "Codesandbox"
})

local Toggle = Tabs.Main:AddToggle("Destroy Ad portal", {
    Title = "Destroy AD portal",
    Description = "This destroys the the annoying pisstake of a portal",
    Default = false,
    Callback = function(state)
        if state then
            print("Toggle On")
        else
            print("Toggle Off")
        end
    end
local Toggle = Tabs.Main.Addtoggle("Destroy Ad Teleport", function()
    local part = workspace:FindFirstChild("RobloxForwardPortals")
    if part then
        part:Destroy()
    end
end)
