local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local Window = Fluent:CreateWindow({
    Title = "MoonHub " .. Fluent.Version,
    SubTitle = "by Moonlight×Godlike",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 460),
    Acrylic = true,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl
})

-- Tabs Setup
local Tabs = {}
Tabs.Main = Window:AddTab({
    Title = "Main",
    Icon = "Anchor"
Main = Window:AddTab({ Title = "Server Stats", Icon = "anchor" }),
Main = Window:AddTab({ Title = "Auto Weights", Icon = "anchor" }),
Main = Window:AddTab({ Title = "Auto kill", Icon = "anchor" }),
Main = Window:AddTab({ Title = "Auto rebirth", Icon = "anchor" }),
Main = Window:AddTab({ Title = "OP tools", Icon = "anchor" }),
Main = Window:AddTab({ Title = "Credits"
})
